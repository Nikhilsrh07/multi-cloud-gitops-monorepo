apiVersion: v1
kind: Service
metadata:
  name: nodejs-app-service
  namespace: production
  labels:
    app: nodejs-multi-cloud-app
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: nodejs-multi-cloud-app
